# 简体中文架构图提示词

使用 `image_gen` 生成适合 README 展示的横向技术架构图。

```text
为“{项目名}”生成专业、清晰的技术架构图。所有标题、节点和说明使用简体中文，仅代码标识符保留英文。展示用户入口、管理端、移动端、服务端、数据库、缓存、Docker 部署、AI 协作层、文档与 assets 资产层。使用白底、清晰线框和箭头，蓝绿橙作为克制强调色。不要渐变、阴影、水印或无关装饰。
```

保存到对应图片类型的 `zh-CN/` 目录。
